schema:
  - '${MC_API_URL}/graphql':
      headers:
        Cookie: mcAccessToken=${MC_ACCESS_TOKEN}
        X-Graphql-Target: ctp
        X-Project-Key: ${INITIAL_PROJECT_KEY}
documents: '**/*.graphql'
