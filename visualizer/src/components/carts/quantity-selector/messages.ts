import { defineMessages } from 'react-intl';

export default defineMessages({
  apply: {
    id: 'Orders.Create.Step.LineItems.QuantitySelector.apply',
    description: 'The apply label for the button',
    defaultMessage: 'Apply',
  },
});
